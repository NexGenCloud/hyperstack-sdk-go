name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Jobs build
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Check out code into the Go module directory
      uses: actions/checkout@v3

    # Set up Go
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.21

    # Get dependencies
    - name: Get dependencies
      run: make install
      
    # Pull latest spec
    - name: Pull latest spec
      run: make pull

    # Generate SDK
    - name: Generate SDK
      run: make generate
      env:
        DIR_PACKAGE: ${{ github.workspace }}/lib

    # Run tests 
    - name: Run tests
      run: make test
